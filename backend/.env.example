# Environment Configuration for Remote Elevator Control System Backend
# Copy this file to .env and update with your actual values

# ============================================
# DATABASE CONFIGURATION
# ============================================
# For local development (PostgreSQL installed locally):
DATABASE_URL="postgresql://remotecon:remotecon_password_change_this@localhost:5432/remotecon?schema=public"

# For Docker development (using docker-compose):
# DATABASE_URL is set automatically in docker-compose.yml

# ============================================
# JWT SECRETS (IMPORTANT: Change these!)
# ============================================
# Generate secure random strings (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_SECRET="your-super-secret-jwt-access-key-min-32-chars-change-this"
JWT_REFRESH_SECRET="your-super-secret-jwt-refresh-key-min-32-chars-change-this"

# ============================================
# JWT EXPIRATION TIMES
# ============================================
JWT_ACCESS_EXPIRATION="15m"
JWT_REFRESH_EXPIRATION="7d"

# ============================================
# SERVER CONFIGURATION
# ============================================
NODE_ENV="development"
PORT="5000"

# ============================================
# CORS CONFIGURATION
# ============================================
# Add all frontend URLs that should be allowed (comma-separated)
CORS_ORIGIN="http://localhost:8100,http://localhost:4200,http://localhost:8080"
